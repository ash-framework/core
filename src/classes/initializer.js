'use strict'

const Base = require('./base')

/**
 *
 * @class Initializer
 * @extends Base
 * @constructor
 */
module.exports = class Initializer extends Base {
  /**
   * @method init
   * @param {Object} app
   */
  init (app) {

  }
}
